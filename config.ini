[DEFAULT]
# 入力データのCSVパス
csv_file = data/input.csv
# 結果を書き出すExcelファイルのパス
output_file = data/result.xlsx
# 探索を打ち切るまでに集める解の最大件数
limit = 100
# 探索の最大実行時間(秒)。当てはまる組み合わせが極端に多いtargetを指定すると
# 無応答のように見えるほど時間がかかることがあるため、ここで打ち切る
search_time_limit = 30
# 数値を表すCSV列名
amount_column = 数値
